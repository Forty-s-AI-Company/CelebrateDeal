# G7-08 Live Studio sellable closure

- Evidence time: `2026-08-08T13:41:20Z`
- Workspace HEAD: `1a8a4bb3acad8aabef30a7d9fbe4dc1488d6a758`
- Mode: `PRELAUNCH_DEV_AUTONOMOUS`, local/disposable only
- Verdict: `LOCAL_PRODUCT_CLOSURE_PASS`
- Production/staging/provider operations: `0`

## Product closure

1. Final publish now parses the complete submitted five-step draft on the server and rejects incomplete payloads or a slug whose canonical value is empty.
2. Live status changes are constrained to the explicit lifecycle; a direct POST cannot jump from `draft` to `ended`.
3. Draft consumption uses one atomic Prisma `updateMany` fence over tenant, live binding, revision, unconsumed state, expiry and the complete JSON payload. A tab cannot publish content different from the saved revision it claims.
4. A fast final submit now writes the latest saved draft ID and revision directly into the hidden controls before `requestSubmit()`. It no longer depends on React committing a state render first.
5. Registration notifications can bind only an active `email` template with trigger `registration_confirmed`. Edit screens warn and unbind an unavailable legacy/SMS/LINE template instead of silently retaining a non-deliverable selection.
6. Product, ready video, form, template, published interaction script, image asset, quota page and existing live references remain tenant scoped. Both custom allocations and member quotas now verify the exact `teamId:membershipId` binding.
7. Invalid final drafts expose a visible `role=alert` message; existing draft save/conflict/pending feedback and publish button loading behaviour remain intact.

## Deterministic verification

- Final targeted Vitest command covered actions, create/edit pages, stepper, draft queue/client/schema, quota policy and message templates: `9 files / 240 tests PASS`, exit `0`.
- An intermediate run exposed one alert-render regression (`237/238`); the implementation was corrected and the complete targeted set was rerun. The failed run is not reported as PASS.
- Scoped ESLint over 19 Live Studio source/test files: `PASS`, exit `0`.
- `npx tsc --noEmit`: `PASS`, exit `0`.
- `git diff --check` on the G7-08 scope: `PASS`; only Git LF/CRLF notices were emitted.
- Staged paths in the G7-08 scope: `0`.

## Disposable PostgreSQL evidence

- Container: loopback `postgres:16-alpine` only.
- Disposable schema: `g708_live_1fcfb1aeeb0244ff`.
- Prisma migration deploy/status: `38/38 migrations PASS`, schema up to date.
- `src/lib/live-studio-draft.db.test.ts`: `1 file / 1 test PASS`.
- The test proved that the same revision with a different JSON payload claims `0` rows, while the exact payload claims `1` row.
- Cleanup: `DROP SCHEMA ... CASCADE` exit `0`; fresh namespace count for `g708_live_%` is `0`.
- Prisma schema SHA-256: `25298a3b9561c6e22b77b60afc48959362f799a4dbf411435c666157f091649c`.
- Combined 38-migration digest: `0a118b04c6900594889173af2dbb5d84d30a6c02e85d83c3ee2f7eb6f8430f38`.

## Current-source production build

- Physical dependency mirror contained `0` `.env*` files.
- The first build with no controlled settings stopped at repository preflight because required deployment settings were absent. It is preserved as `EXPECTED_PREFLIGHT_FAIL`, not a product PASS or product failure.
- A different, bounded path then loaded the repository's synthetic `config/build-env.controlled.json`; Sentry upload and Next telemetry were disabled, no provider or database connection was made.
- Final current-source build after all reviewer remediations: Next.js `16.2.11` Turbopack compile, TypeScript, page collection and `98/98` static pages `PASS`, exit `0`.

## Review and residual evidence

- Read-only AI reviewer found no P0 and identified two P1 issues: member-quota team binding and empty canonical slug. Both were corrected and covered by new action regressions before the final `240/240` run and build.
- The main review additionally found the fast-submit hidden draft claim race; it was corrected and its regression now asserts the submitted ID/revision values.
- Chrome/Bombmy browsing was not retried because the existing binding path is terminal `TOOL_BLOCKED_NOT_RUN`.
- Real Cloudflare Live Input/Stream, external Email delivery, staging desktop/mobile and provider callback evidence are `NOT_RUN`; none are represented as PASS.

## Score, blockers and rollback

- Live Studio local functional candidate: `8.0/10` and eligible for the Goal's per-function `>=7` inventory threshold.
- Canonical CAT01-CAT10 total remains `73.5`; this WP has `canonical_delta=0` because external Browser/provider evidence and CAT04/CAT10 human evidence remain incomplete.
- No manual action is needed for this local closure. Later human/external items remain separate: legal/finance/release acceptance, staging QA, and provider sandbox evidence.
- Rollback scope is limited to the G7-08 hunks listed in the source manifest plus the new DB regression/evidence files. No migration or production data rollback is required.
- Next highest-value work: G7-09 merchant operations closure, starting with executable merchant onboarding, support/refund handoff and high-risk async feedback; human CAT10 signatures stay deferred and must not block local product improvements.

## Integrity

- Source manifest: `docs/ai-team/evidence/g7-08-live-studio-source-manifest-20260808.txt`
- Source manifest SHA-256: `c7d6b10586291c53d67fa25bb8fceb1868b7bcd68d8752da4345a6fc2de38028`
- No commit, push, merge or deployment was performed.
