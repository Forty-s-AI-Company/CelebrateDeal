# CelebrateDeal 目前版本與上線缺口

更新：2026-09-26（Asia/Taipei）。本頁是 staging 驗收入口；每項 PASS 必須對應當次部署與執行證據。下方 2026-09-25 快照只供追溯，不能當成目前待辦或新版驗收。

## 2026-09-26 現況

- 指定 [staging 網址](https://celebrate-deal-staging.carry-digital-nomad.in.net) 綁在 `codex/staging-release-20260926` Preview 分支；部署會隨該分支前進，不能把本頁列出的舊 immutable host 當成當前 alias。最近一次 UI 更新的 GitHub Deployment source 是 `ec5b0eb7acc8397f9334e8308eacd114a8f29743`，當次 Preview `celebrate-deal-staging-jyowjeceo-a25814740s-projects.vercel.app` 為 Ready、alias 已綁定，且與當時 master `6b8c49bdb822b31708defd1cc17f162774a3d65b` 的 Git tree 相同；公開 `/api/health` 回應 200。公告 v2 已在固定網址的 Chrome 畫面核對。後續每次部署仍須重新核對來源與 alias；當前主機以 Vercel 部署紀錄為準。
- 最近一次完整 **PASS** 的受保護瀏覽器 [run 36218272116](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36218272116) 屬前一個同應用版本來源 `35b9382fef465624d575609eb5beab2b58fdfeb5`：desktop/mobile 各五頁 HTTP 200，Dashboard KPI 與明細可見，導覽與局部編輯互動通過，兩個合成 session 均撤銷。公告 v2 更新後，同一固定站來源的 [run 36220227789](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36220227789) 與 [run 36220408695](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36220408695) 仍有十頁、資料與互動通過，卻各有一次關鍵 JavaScript／CSS 資源載入失敗，總結果為 **BLOCKED**。診斷分類工具由 [PR #323](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/323) 合入 master `800abee64aa7954ed0cc7c24abf136e46c328b83`；對同一個固定站來源的 [最新 run 36221861858](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36221861858) 已驗證 lineage／alias，但在 desktop Dashboard 的 `PAGE_NAVIGATION` 遇到 `TIMEOUT`，未完成任何頁面旅程，該次關鍵資源失敗數為 0，合成 session 已撤銷。這筆逾時不能解釋先前兩次資源失敗，也不能沿用前一版 PASS。煙測本身不涵蓋 Funnel 建立／發布、付款、圖片上傳或寄信。
- [PR #327](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/327) 已合入 master。使用同一固定站來源與受保護診斷的 [run 36247730669](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36247730669) 仍是 **BLOCKED**：Dashboard 主文件 2xx、外框與 KPI 已出現，明細尚未出現，`readyState=loading`，20 秒內未觸發 `DOMContentLoaded`；該次沒有 page error、同站 5xx 或關鍵 JS／CSS 失敗，合成 session 已撤銷。這支持明細串流尾段延遲的診斷，不代表先前兩次關鍵資源載入失敗已消失。[PR #328](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/328) 將明細的獨立資料庫讀取改成有上限的並行批次，仍待受保護 CI、合併與固定站新來源煙測。固定站完整旅程維持 **BLOCKED**。
- GitHub staging Environment 已有 `VERCEL_TOKEN`，Vercel Preview 的 R2 五項與 Stream 三項設定存在。使用者同意空的 `celebrate-deal-staging` bucket 啟用公開 r2.dev 開發網址，僅供合成 staging 資料。[較新來源唯讀 provider run 36220235179](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36220235179) 成功；此前 [收據 36218278700](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36218278700) 明列 R2=`ok`、Stream=`ok`、`nonProductionScope=unverified`。這仍不是應用程式圖片上傳／公開讀取或 Stream 非正式資源範圍的端到端證據。
- Cloudflare UI 已核對 `celebrate-deal-staging` bucket 為空、公開 r2.dev 開發網址啟用、staging R2 憑證限定該 bucket。原本沒有 CORS 原則；2026-09-26 已成功加入僅允許固定 staging origin 以 `PUT` 與 `Content-Type` 使用預簽上傳的規則。這是設定狀態，**不是上傳／公開讀取 PASS**；真實商品圖片流程的受保護驗證在 [PR #326](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/326) 待 CI。Cloudflare UI 中可見的 Stream／Images token 資源範圍為所有帳戶，但無法在不讀 Secret 的條件下確認 Vercel 綁定的是哪一筆；Stream `nonProductionScope` 仍是 **UNVERIFIED**。
- PayUni 仍限 Sandbox。[舊買家訂單 run 36142862467](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36142862467) 曾證明當時來源的付款、callback、持久化訂單與重複 callback 冪等性，不能轉算成目前來源的 PASS。[新來源一次固定合成交易 run 36217020374](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36217020374) 在 `00099f7e3b3c8a7e923047e1ab72a827fcb78e4c`（與當時 master 同 tree）建立付款嘗試並提交一次 Sandbox 表單，停在 `PAYMENT_CONFIRMATION_AMBIGUOUS`；付款、callback 與已付款訂單均未驗證，退款未送出。先用唯讀方式釐清該筆交易，再決定是否需要新的測試；目前 `wp4-buyer-payment-check` API 尚未實作，舊 [唯讀檢查 run 36211458633](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36211458633) 因此無法完成。
- 固定買家唯讀查單 API 已在 [PR #325](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/325) 實作並補 API 契約，尚待受保護 CI、合併、staging 更新與一次精確來源的唯讀收據。在那之前不能宣稱未明交易已釐清，也不能提交新付款或退款。
- 舊功能 [PR #210](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/210)／[#211](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/211) 仍有 conflicts 且 quality 失敗，需逐功能整合；過時的 PR #1 已關閉，不能宣稱所有舊分支都已合併。下一輪優先處理 Sandbox 未明交易的唯讀確認、固定站 Funnel 建立／保存／公開旅程，以及 R2 圖片端到端與 Stream 非正式範圍。`CORE_STAGING_READY` **尚未成立**，Goal 不應標 complete。

## 2026-09-25 歷史快照

### 來源與站台

- 本輪應用程式基準為 [PR #316](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/316) 合併後的 `556e9ee54efa74c64657c0d1e576ba0c0fa34bec`；兩組受保護 `quality` 與 Preview 檢查通過。其中一條 CI 首次因一個 Playwright flaky 測試失敗，保留 `--fail-on-flaky-tests` 後僅重跑一次並通過。[staging migration apply](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36121750590) 已對同一固定資料庫完成 58→79 筆，目標欄位與 schema 核對通過；後續程式合併未新增 migration。
- 受保護 PR #274–#283、#286–#298 已合進 master；本次盤點的應用程式基準 [PR #298](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/298) 的 merge SHA 為 `a2969b0523cf6bb15284c669699a5f9b7f76120e`，[該 SHA 的 CI](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36094533079) 已通過。[PR #304](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/304) 已將 staging 驗證工具合入 master，merge SHA 為 `1d8bac1cc5ed559a5b17428afae70cb4bbb21ec7`；兩條受保護 `quality` 與 Vercel Preview 檢查通過。這些 CI 證據只適用於對應 source，尚不代表指定 staging 已重新部署。[受保護 Preview 身分檢查](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36035415243) 成功。
- 指定 [staging 網址](https://celebrate-deal-staging.carry-digital-nomad.in.net) 已切至 Ready 的 Preview deployment `dpl_FkouHS2QGr7YMUs8D2g8QBYfBsd4`，immutable host 為 `celebrate-deal-staging-dkgxp7g1l-a25814740s-projects.vercel.app`。GitHub Deployment source `fa7368405ac231e115ebee6831f6fab2b724f855` 與 PR #316 合併後 master `556e9ee54efa74c64657c0d1e576ba0c0fa34bec` 的 Git tree 同為 `7b6716d4eb98e5e4511a3abc5184726aa4095b8d`。Vercel CLI 核對 alias 指向該 Preview，公開健康檢查正常；登入後受保護瀏覽器流程仍缺可用的 CI `VERCEL_TOKEN`，未完成驗證。
- 固定網址的 `/`、`/login`、`/api/health` 回應 200，health 回報 `ok=true`、`database=ok`；未授權 `/api/admin/preflight` 回應 401。受保護 workflow 以既有 `JOB_SECRET` 對 immutable Preview 驗證 Supabase 公開 URL、執行期／migration／staging DB identity 與 DB 可連線，僅輸出布林結果，全部通過。這些證據不等於登入、Funnel 或付款旅程通過。
- 匿名真實瀏覽器在桌機及手機的首頁與合成商品結帳頁取得 200，未觀察到 page error 或同源 5xx；`/login` 的 HTTP 檢查為 200，手機瀏覽器也取得 200，桌機瀏覽器登入頁導覽結果未能判定。尚未涵蓋登入後頁面。
- [受保護唯讀診斷 run 36059197795](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36059197795) 在套用前證實 staging DB 的 58 個已完成 Prisma migration 是固定來源 79 個的完整前綴，缺最後 21 個；無 unresolved failure、未知已套用項或 checksum mismatch。WP4 fixture 的唯讀 preflight 為 `READY`。這尚不能證明 fixture POST 503 的唯一根因。
- [受保護唯讀診斷 run 36068519165](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36068519165) 在套用前確認 `Vendor.enabledFeatureModules` 當時在固定 staging DB **不存在**，且仍缺精確的最後 21 個 migration；fixture preflight 仍為 `READY`。登入後共用版型讀取該欄位，故這是內容缺失的明確 schema 阻擋；實際 runtime 例外與 Sandbox fixture 503 的唯一根因仍未單獨證明。
- [首次受保護隔離重播 run 36082790275](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36082790275) 在已知歷史 rollback 的基準檢查安全停止；[第二次 run 36085141048](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36085141048) 在還原後摘要比對停下。PR #293／#294 分別修正精確 rollback 基準與跨 PostgreSQL 排序規則的表筆數比對。[第三次受保護 run 36086949395](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36086949395) 的 sanitized receipt 為 `PASS`：58 筆來源 history 與還原摘要相符，21 個 pending SQL 在無網路的一次性容器全部重播，目標 schema 與原 history 通過；來源 staging `databaseWrites=0`。這證明本次快照的 SQL 相容性，不代表固定 staging 已套用 migration，也不是可保留回復備份。
- PayUni 保持 Sandbox。[runner binding preflight](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36130730466) 已確認 JOB 與測試卡欄位存在；[受保護買家訂單 run 36142862467](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36142862467) 已在同一固定 Preview 完成一次付款、Return callback、持久化訂單回讀及一次重複 Notify callback 的冪等性核對。回執整體仍為 `BLOCKED/RECONCILE_REJECTED`：退款請求 1 次、12 次對帳未確認完成，`refunds=0`；不得重送付款或退款，退款／對帳另列未完成。
- [受保護 provider 唯讀 run 36144259044](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36144259044) 對目前固定 staging 的精確 immutable Preview source 探測：sanitized receipt 為 `BLOCKED/PROVIDER_NOT_REACHABLE`，五項 R2 欄位均未設定，Stream=`ok`、`nonProductionScope=unverified`；未執行 provider 寫入，也不能證明 Stream token 的非正式資源範圍。
- `vercel env run` 無法讀回寫入後不可見的 Secret 值；先前文件由此推論資料庫設定缺失是錯誤的。以上受保護 runtime 檢查已取代那項推論，不要求使用者重提供既有 Secret。

### 當時驗收狀態

| 範圍 | 狀態 | 尚需的證據 |
| --- | --- | --- |
| master 與部署來源 | 已驗證 | PR、CI、GitHub Deployment lineage、Vercel alias／deployment 對應如上；後續新部署須重驗 |
| DB／Supabase project identity | 已驗證 | 受保護 Preview 身分檢查通過；Auth、Storage 等實際使用資源仍須各自核對 |
| 固定 Preview PayUni Sandbox 設定綁定 | **買家交易已驗證** | [runner 前置 run 36130730466](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36130730466) 確認必要 CI 欄位存在；[目前受保護買家 run 36142862467](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36142862467) 已由實際 Sandbox 付款與 callback 證實買家路徑可用。退款／對帳另列，不推論 Production 商家設定 |
| R2／Cloudflare Stream 非正式資源綁定 | **BLOCKED／NOT_PROVEN** | [目前精確來源唯讀探測](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36144259044)：五項 R2 設定均缺，Stream 可連線但非 Production scope 未核對；需在設定完成後對精確來源重驗，不把 R2 上傳或 Stream 範圍標 PASS |
| 固定 staging 核心瀏覽器旅程 | **NOT_PROVEN** | 合成資料的登入、Funnel 建立／保存／發布與公開表單、商品、影片、行動版與關鍵 console／5xx；本機 Playwright CI 不能替代固定站驗收 |
| 固定 staging 登入後頁面煙測 | **BLOCKED** | [最近一次受保護 run 36130729888](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36130729888) 的 sanitized receipt 明確為 `VERCEL_TOKEN_MISSING`；合成 session 與瀏覽器旅程均未執行。需在受保護 staging Environment 提供 Vercel alias 查證憑證後，對目前精確來源重跑；本機 CLI 核對不能替代 CI gate |
| PayUni Sandbox 買家訂單 | **PASS：付款至重複 callback** | [受保護 run 36142862467](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36142862467) 的已驗 sanitized receipt：`payments=1`、`returnCallbackMapped=true`、`orderPersisted=true`、`duplicateCallbackVerified=true`；訂單 proof 要求 paid 訂單一筆、paid event 一筆、庫存 reservation 已提交，重播後 proof 完全相同。這不代表退款／對帳完成 |
| Sandbox runner 前置關卡 | **PASS，僅 runner 綁定** | [受保護 binding preflight 36130730466](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36130730466) 已通過來源檢查、contract、Prisma Client 生成及必要 CI Sandbox 欄位存在性；不代表 app fixture 或付款可用 |
| 固定 Sandbox 交易與退款／對帳 | **買家閉環 PASS；退款 BLOCKED** | [目前受保護 run 36142862467](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36142862467) 已越過 fixture、admission、checkout、付款及 callback；但 `refundPosts=1`、`reconcilePosts=12`、`refunds=0`，整體為 `RECONCILE_REJECTED`。既有合成交易狀態不明時須先唯讀查詢或安全續接，不可重送付款或退款 |
| staging Prisma migration | **PASS：79／79** | [受保護 apply run 36121750590](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36121750590) 回執為 `PASS`，同一固定 staging DB 的 `preCount=58`、`postCount=79`、`migrationAttempted=true`、`columnVerified=true`、`schemaVerified=true` |
| staging 隔離 migration 相容性 | **PASS，僅一次性還原** | [受保護 run 36121518282](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36121518282) 與 apply 所用 master `69cb10a31c0b0975bab94cea6fe8b73d80fd5aee` 精確綁定，21 個 pending SQL、目標 schema 與來源 history 均通過；演練本身對固定 DB 寫入為 0 |
| staging migration apply | **PASS** | [run 36121750590](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36121750590) 已實際套用並核對 schema；此前 URL 查詢參數阻擋由 [PR #311](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/311) 的有界白名單解決 |
| staging `public` schema 隔離還原 | **演練 PASS，無保留備份** | [第一次 run 36059227552](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36059227552) 與合成 session 並行，筆數摘要不符；[單獨重跑 36059754159](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36059754159) 的 sanitized receipt 證實 dump／隔離還原的 schema、extensions、58 筆 migration 與各表筆數一致，staging 寫入為 0。PR #288 已提供加密保留備份程式，但 staging 專用的 `STAGING_BACKUP_AGE_RECIPIENT` Environment variable 仍不存在，尚未產生可保留的加密 artifact 或 recovery receipt；舊 dump 已清除，不能當事後回復備份 |
| 舊 PR #210／#211 | 待按功能整合 | 兩者仍有衝突及獨有功能；清單見 [Git 盤點](integration-inventory-20260924.md)，不能宣稱「全部合完」 |
| Production | 未評定 | 正式部署、正式資料與正式金流不在本輪授權內 |

依 [Goal Plan](goal-plan-20260924.md)，本輪必需的 `CORE_STAGING_READY` **尚未成立**。固定 staging 使用已合併且經驗證的 PR #316 應用程式來源，資料庫 migration 79／79；PayUni Sandbox 的成功付款、callback、持久化訂單及重複 callback 已有同一部署的受保護證據。仍缺固定站登入後核心瀏覽器旅程（CI `VERCEL_TOKEN` 未設定）、R2 非 Production 設定與 Stream scope 查證；Sandbox 退款／對帳另有未完成交易狀態。Codex Goal 不應標為 complete。

固定 staging 原缺的 21 個 migration 已經靜態複審、隔離 replay，並由受保護 apply 從 58／79 套用與核對至 79／79；這些驗證不代表有可保留的回復備份。未來若增加 migration，須重新取得同來源的 replay、鎖控與 apply 證據，不沿用這次收據。目前下一步是補齊 CI 的 Vercel alias 驗證憑證並完成登入後核心旅程、設定非 Production R2 並核實 Stream scope，以及對已付款的合成交易先查詢／安全續接退款狀態；不得重送付款或退款。較細功能見 [下一輪清單](NEXT-CYCLE.md)，歷史 checkpoint 見 [執行紀錄](goal-progress-20260924.md)。
