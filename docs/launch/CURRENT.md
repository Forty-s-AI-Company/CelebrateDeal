# CelebrateDeal 目前版本與上線缺口

更新：2026-09-25（Asia/Taipei）。本頁是目前 staging 驗收的入口；每項 PASS 必須對應當次部署與執行證據。過去的工作紀錄保留原始結論，不自動升級為新版驗收。

## 來源與站台

- 目前 master 為 [PR #312](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/312) 合併後的 `896bd89646e494843e7e3c27749d1cc8c102b5a5`，兩組受保護 `quality` 與 Preview 檢查通過。[staging migration apply](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36121750590) 已對同一固定資料庫完成 58→79 筆，目標欄位與 schema 核對通過；其後合併未新增 migration。
- 受保護 PR #274–#283、#286–#298 已合進 master；本次盤點的應用程式基準 [PR #298](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/298) 的 merge SHA 為 `a2969b0523cf6bb15284c669699a5f9b7f76120e`，[該 SHA 的 CI](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36094533079) 已通過。[PR #304](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/304) 已將 staging 驗證工具合入 master，merge SHA 為 `1d8bac1cc5ed559a5b17428afae70cb4bbb21ec7`；兩條受保護 `quality` 與 Vercel Preview 檢查通過。這些 CI 證據只適用於對應 source，尚不代表指定 staging 已重新部署。[受保護 Preview 身分檢查](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36035415243) 成功。
- 指定 [staging 網址](https://celebrate-deal-staging.carry-digital-nomad.in.net) 已切至 Ready 的 Preview deployment `dpl_GpXrnsWGJD7TtUMpmNbWe2o2n6Qu`，immutable host 為 `celebrate-deal-staging-336vi0gc4-a25814740s-projects.vercel.app`。GitHub Deployment source `fecab1bb0cd9a0216711863aa139bc5d5cadeea9` 與合併後 master `896bd89646e494843e7e3c27749d1cc8c102b5a5` 的 Git tree 同為 `ca064bc18817b4cbfea0a1af3c117dfedc1abadf`。Vercel CLI 核對 alias 指向該 Preview，公開健康檢查正常；受保護瀏覽器流程因 CI 缺 `VERCEL_TOKEN` 停在 alias gate，尚未登入。
- 固定網址的 `/`、`/login`、`/api/health` 回應 200，health 回報 `ok=true`、`database=ok`；未授權 `/api/admin/preflight` 回應 401。受保護 workflow 以既有 `JOB_SECRET` 對 immutable Preview 驗證 Supabase 公開 URL、執行期／migration／staging DB identity 與 DB 可連線，僅輸出布林結果，全部通過。這些證據不等於登入、Funnel 或付款旅程通過。
- 匿名真實瀏覽器在桌機及手機對 `/`、`/login` 均取得 200，未觀察到 console error、page error 或 5xx；尚未涵蓋登入後頁面。
- [受保護唯讀診斷 run 36059197795](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36059197795) 在套用前證實 staging DB 的 58 個已完成 Prisma migration 是固定來源 79 個的完整前綴，缺最後 21 個；無 unresolved failure、未知已套用項或 checksum mismatch。WP4 fixture 的唯讀 preflight 為 `READY`。這尚不能證明 fixture POST 503 的唯一根因。
- [受保護唯讀診斷 run 36068519165](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36068519165) 在套用前確認 `Vendor.enabledFeatureModules` 當時在固定 staging DB **不存在**，且仍缺精確的最後 21 個 migration；fixture preflight 仍為 `READY`。登入後共用版型讀取該欄位，故這是內容缺失的明確 schema 阻擋；實際 runtime 例外與 Sandbox fixture 503 的唯一根因仍未單獨證明。
- [首次受保護隔離重播 run 36082790275](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36082790275) 在已知歷史 rollback 的基準檢查安全停止；[第二次 run 36085141048](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36085141048) 在還原後摘要比對停下。PR #293／#294 分別修正精確 rollback 基準與跨 PostgreSQL 排序規則的表筆數比對。[第三次受保護 run 36086949395](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36086949395) 的 sanitized receipt 為 `PASS`：58 筆來源 history 與還原摘要相符，21 個 pending SQL 在無網路的一次性容器全部重播，目標 schema 與原 history 通過；來源 staging `databaseWrites=0`。這證明本次快照的 SQL 相容性，不代表固定 staging 已套用 migration，也不是可保留回復備份。
- PayUni 保持 Sandbox。[新版 runner binding preflight](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36130730466) 已確認受保護 CI 的 JOB 與 Sandbox 測試卡綁定存在；這只證明 runner 可取得設定，不證明商家憑證有效。[實際 Sandbox run](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36130889971) 在固定 Preview 的合成 fixture POST 回傳 `FIXTURE_HTTP_REJECTED`，checkout、付款與退款均為 0；尚無成功 callback 或訂單證據。[PR #313](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/313) 正為該 503 增加不洩漏資料的階段分類，尚未合併或重跑。
- [受保護 provider 唯讀 run 36124368563](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36124368563) 對新版固定 staging 的 immutable Preview source 探測：sanitized receipt 為 `BLOCKED`，五項 R2 欄位均未設定，Stream=`ok`、`nonProductionScope=unverified`；未執行 provider 寫入，也不能證明 Stream token 的非正式資源範圍。
- `vercel env run` 無法讀回寫入後不可見的 Secret 值；先前文件由此推論資料庫設定缺失是錯誤的。以上受保護 runtime 檢查已取代那項推論，不要求使用者重提供既有 Secret。

## 驗收狀態

| 範圍 | 狀態 | 尚需的證據 |
| --- | --- | --- |
| master 與部署來源 | 已驗證 | PR、CI、GitHub Deployment lineage、Vercel alias／deployment 對應如上；後續新部署須重驗 |
| DB／Supabase project identity | 已驗證 | 受保護 Preview 身分檢查通過；Auth、Storage 等實際使用資源仍須各自核對 |
| 固定 Preview PayUni Sandbox 設定綁定 | **部分已驗證** | [目前 runner 前置 run 36130730466](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36130730466) 確認 CI 的 JOB 與 Sandbox 測試卡欄位存在；[先前 app 綁定 run 36080495339](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36080495339) 屬舊 Preview。當前商家憑證有效性與交易成功仍未證實 |
| R2／Cloudflare Stream 非正式資源綁定 | **BLOCKED／NOT_PROVEN** | [新版固定來源探測](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36124368563)：五項 R2 設定均缺，Stream 必要欄位存在但非 Production scope 未核對；R2 上傳與 Stream 資源綁定不能標 PASS |
| 固定 staging 核心瀏覽器旅程 | **NOT_PROVEN** | 合成資料的登入、Funnel 建立／保存／發布與公開表單、商品、影片、行動版與關鍵 console／5xx；本機 Playwright CI 不能替代固定站驗收 |
| 固定 staging 登入後頁面煙測 | **BLOCKED** | [目前受保護 run 36130729888](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36130729888) 的 sanitized receipt 明確為 `VERCEL_TOKEN_MISSING`；合成 session 與瀏覽器旅程均未執行。需在受保護 staging Environment 提供 Vercel alias 查證憑證後重跑；本機 CLI 核對不能替代 CI gate |
| PayUni Sandbox 買家訂單 | **NOT_PROVEN** | 同一固定部署上一筆成功付款、callback、持久化訂單、使用者可見狀態及重複 callback 冪等性；退款／對帳屬更完整的財務閉環 |
| Sandbox runner 前置關卡 | **PASS，僅 runner 綁定** | [受保護 binding preflight 36130730466](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36130730466) 已通過來源檢查、contract、Prisma Client 生成及必要 CI Sandbox 欄位存在性；不代表 app fixture 或付款可用 |
| 固定 Sandbox 交易嘗試 | **BLOCKED_BEFORE_PAYMENT** | [目前受保護 run 36130889971](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36130889971) 的 sanitized receipt 為 `FIXTURE_HTTP_REJECTED`；`fixturePosts=1`，`checkoutPosts=0`、`payments=0`、`refunds=0`。固定 DB migration 已 79／79，須先依 [PR #313](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/313) 的有限分類找出 fixture 503 根因，不能直接重送付款 |
| staging Prisma migration | **PASS：79／79** | [受保護 apply run 36121750590](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36121750590) 回執為 `PASS`，同一固定 staging DB 的 `preCount=58`、`postCount=79`、`migrationAttempted=true`、`columnVerified=true`、`schemaVerified=true` |
| staging 隔離 migration 相容性 | **PASS，僅一次性還原** | [受保護 run 36121518282](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36121518282) 與 apply 所用 master `69cb10a31c0b0975bab94cea6fe8b73d80fd5aee` 精確綁定，21 個 pending SQL、目標 schema 與來源 history 均通過；演練本身對固定 DB 寫入為 0 |
| staging migration apply | **PASS** | [run 36121750590](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36121750590) 已實際套用並核對 schema；此前 URL 查詢參數阻擋由 [PR #311](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/311) 的有界白名單解決 |
| staging `public` schema 隔離還原 | **演練 PASS，無保留備份** | [第一次 run 36059227552](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36059227552) 與合成 session 並行，筆數摘要不符；[單獨重跑 36059754159](https://github.com/Forty-s-AI-Company/CelebrateDeal/actions/runs/36059754159) 的 sanitized receipt 證實 dump／隔離還原的 schema、extensions、58 筆 migration 與各表筆數一致，staging 寫入為 0。PR #288 已提供加密保留備份程式，但 staging 專用的 `STAGING_BACKUP_AGE_RECIPIENT` Environment variable 仍不存在，尚未產生可保留的加密 artifact 或 recovery receipt；舊 dump 已清除，不能當事後回復備份 |
| 舊 PR #210／#211 | 待按功能整合 | 兩者仍有衝突及獨有功能；清單見 [Git 盤點](integration-inventory-20260924.md)，不能宣稱「全部合完」 |
| Production | 未評定 | 正式部署、正式資料與正式金流不在本輪授權內 |

依 [Goal Plan](goal-plan-20260924.md)，本輪必需的 `CORE_STAGING_READY` **尚未成立**。固定 staging 已指向與目前 master 同 Git tree 的新部署，資料庫 migration 79／79 已通過；但登入後內容驗收被 CI 缺少 `VERCEL_TOKEN` 擋住，Sandbox runner 綁定雖通過，實際 fixture 在付款前回傳 503，R2 設定全缺且 Stream 非 Production scope 未核實。成功付款、callback、持久化訂單及重複 callback 冪等性證據仍缺，Codex Goal 不應標為 complete。

固定 staging 的 21 個待套用 SQL 經獨立靜態複審，未發現刪表、刪欄、既有欄位型別轉換或資料改寫；主要風險是既有表建立索引時的鎖等待及持鎖時間。先前以 `ALL_DISPOSABLE_SYNTHETIC` 單一輸入擋下所有 schema 更新，對這批增量 SQL 過於嚴格，且字串本身不能證明資料性質。受保護 apply 流程改以固定 staging project、精確來源與 migration tree、當前 master 的隔離 replay receipt、套用前後 history／schema 核對及 Prisma 連線 URL 的五秒鎖等待與六十秒語句限制控管。此調整不表示有可保留備份。後續固定 staging apply 已成功，固定 DB 現為 79／79，驗證見上表。若未來新增 migration，仍須重新取得對應來源的 replay、鎖控及 apply 證據，不以這次收據代替。舊本機 `codex/staging-migration-apply-gate` 草稿已被複審否決；[PR #299](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/299) 的原始保護流程已納入 [PR #304](https://github.com/Forty-s-AI-Company/CelebrateDeal/pull/304)。PR #304 另整合唯讀 provider 探測、登入後手機／桌機與 React 互動煙測，以及 PayUni Sandbox 訂單／重複 callback 證據工具；目前都尚無固定 staging 的外部 PASS 收據。資料庫已完成 migration，仍須補上同一來源的登入後核心瀏覽器旅程，並以有限診斷定位 fixture 503 的根因。只有 fixture 根因解決後才考慮下一次 Sandbox 交易，且需將成功付款、callback、訂單 DB readback／重複 callback 冪等性串在同一 lineage。未證實的項目保持 `NOT_PROVEN`。較細的未製作功能見 [下一輪清單](NEXT-CYCLE.md)；歷史 checkpoint 見 [執行紀錄](goal-progress-20260924.md)。
