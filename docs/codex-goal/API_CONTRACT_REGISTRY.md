# CelebrateDeal API Contract Registry

最後更新：2026-08-25（Asia/Taipei）

基準 revision：`9cd7473`

## 共用契約

- 所有 `readJsonBody`／`readFormDataBody` 呼叫預設以 64 KiB 上限讀取 request body；超量、畸形或無法讀取的內容會交由 Zod schema 正規化為 400。
- Cookie-authenticated JSON write route 先驗證 same-origin 與 `x-celebratedeal-client: web`；Server Actions 另由 `assertServerActionSecurity` 統一驗證。
- `JOB_SECRET` 與 `CRON_SECRET` 都使用 Bearer token、嚴格兩段解析與 timing-safe comparison；沒有設定時 fail closed。`CRON_SECRET` 供核准的 scheduler（Production 若啟用 Vercel Cron）使用 `GET` 排程入口，`JOB_SECRET` 保留給受控人工／job runner 的 `POST` 入口；Preview 不註冊 Vercel Cron。
- Provider webhook 使用原始 body 驗證簽章，再做 runtime schema validation。
- 受保護資源的 tenant／ownership 判斷位於 route 或 domain layer；不存在、跨 tenant 與拒絕存取盡量回傳相同 404，避免資源探測。
- 每條 route 都有同路徑 `route.test.ts`。本 registry 另外標示仍需 DB／browser／外部平台證據的 contract。

## Route handlers

| # | Route／method | Caller 與安全邊界 | Input contract | Tenant／resource boundary | Side effect 與 replay contract | Response／error contract | 目前證據 |
|---:|---|---|---|---|---|---|---|
| 1 | `GET /(app)/billing/invoices/export` | `requireVendorContext` | 無 body | query 固定目前 `vendorId` | 只讀 CSV export；公式字元 neutralization | CSV attachment；不回傳其他 vendor | 同路徑 unit |
| 2 | `GET /admin/billing/payouts/[id]/csv` | `requireFinanceAdmin` + platform MFA | bounded route param | platform payout batch；不存在與拒絕同為 404 | 只讀 CSV export | CSV attachment 或安全 404 | 同路徑 unit |
| 3 | `POST /api/admin/ops/cloudflare/direct-upload`／`GET /api/admin/ops/cloudflare/direct-upload` | timing-safe `JOB_SECRET` | POST 使用 Zod `DirectUploadRequest`，64 KiB；Preview 可從 server-owned `SMOKE_VENDOR_ID` 補 fixture vendor；GET 僅接受 bounded `videoId` query | Cloudflare account 由 server env 綁定；Production 永不套用 fixture vendor fallback | POST 建立一次 provider direct-upload operation；GET 只讀 webhook 更新後的 bounded ready status | 400／401／404／502 使用泛化錯誤；不回傳 provider secret、UID 或原始 payload | 同路徑 unit + staging upload／webhook ready gate |
| 4 | `POST /api/admin/ops/cloudflare/live-input` | timing-safe `JOB_SECRET` | Zod `LiveInputRequest`，64 KiB | Production Cloudflare account 由 server env 綁定 | 建立一次 provider live input；無 client idempotency key | 400／401／502 使用泛化錯誤 | 同路徑 provider fixture；exact binding 為人工 gate |
| 5 | `POST /api/admin/ops/test-analytics` | timing-safe `JOB_SECRET` | 無外部可控 payload | server-selected synthetic event | 每次呼叫送出一筆測試 analytics event；沒有 replay key | 401 或 `{ ok, result }` | 同路徑 unit；delivery 為人工 gate |
| 6 | `POST /api/admin/ops/test-email` | timing-safe `JOB_SECRET` | Zod bounded payload，64 KiB；收件人必須等於受控設定 | 不接受任意收件人 | 每次成功呼叫寄送一封測試信；沒有 replay key | 400／401／403／503／502 使用泛化錯誤 | 同路徑 unit；delivery 為人工 gate |
| 7 | `POST /api/admin/ops/test-monitoring` | timing-safe `JOB_SECRET` | 無 body | server-selected synthetic issue | 每次呼叫建立一次 Sentry test event | 401 或 `{ ok }` | 同路徑 unit；delivery 為人工 gate |
| 8 | `GET /api/admin/preflight` | timing-safe `JOB_SECRET` | 無 body | platform runtime metadata only | 只讀，不揭露 secret value | 401 或 bounded readiness JSON | 同路徑 unit |
| 9 | `POST /api/affiliate-clicks` | same-origin + client marker + 60/min rate limit | Zod bounded JSON，64 KiB；`referralCode` 或共享播放 URL 的 `sourcePage` 至少一項 | vendor、active live、source page 與 referral 都由 DB 驗證；visitor ID 為 server cookie | append click、team attribution 與 HttpOnly cookies；沒有 client replay key | 400／403／404／429 或 `{ ok }` | route/domain negative unit；共享播放來源頁回歸已補；D-002 仍待產品決策 |
| 10 | `POST /api/analytics` | same-origin + client marker + 120/min rate limit | Zod bounded event union，64 KiB | vendor/live pair 與 product/live binding 必須存在 | append-only analytics event；沒有 client replay key | 400／403／404／429 或 `{ ok }` | route lifecycle/product binding unit；D-001 尚待產品決策 |
| 11 | `POST /api/auth/password-reset/request` | same-origin + client marker + 5/min rate limit | Zod email schema，64 KiB | account existence 不回傳給 caller | revoke 舊 token、建立新 token、寄送受控 Email；回應不洩漏 account existence | 400／403／429 或固定成功訊息 | unit；Email delivery 為人工 gate |
| 12 | `POST /api/auth/password-reset/confirm` | same-origin + client marker + 10/min rate limit | Zod token/password schema，64 KiB | token hash + expiry + unused predicate | DB conditional claim；同一 token 只能成功一次；成功後 revoke sessions | 400／403／429 或 `{ ok }` | isolated PostgreSQL sequential + concurrent regression |
| 13 | `POST /api/cloudflare/direct-upload` | timing-safe `JOB_SECRET` | Zod `DirectUploadRequest`，64 KiB | server-side provider account | 與 admin ops direct-upload 相同；每次呼叫可能建立新 provider operation | 泛化 4xx／5xx | 同路徑 provider fixture |
| 14 | `POST /api/cloudflare/live-inputs` | timing-safe `JOB_SECRET` | Zod `LiveInputRequest`，64 KiB | server-side provider account | 與 admin ops live-input 相同；無 client idempotency key | 泛化 4xx／5xx | 同路徑 provider fixture |
| 15 | `POST /api/cloudflare/stream-webhook` | official raw-body signature | raw body 64 KiB + Zod payload | provider UID 必須唯一對應本地 Video；ambiguous mapping fail closed | conditional status claim；`ready`／`error` 不被 stale `processing` 降級，`ready` 可從 `error` 復原 | 400／401／409／413 或 `{ ok, updated }` | unit + isolated PostgreSQL route regression |
| 16 | `POST /api/form-submissions` | JSON same-origin + client marker；native form same-origin browser semantics；10/min rate limit | JSON／form data 64 KiB；form-config allowlist；最多 32 欄、每欄 2,000 chars | active form；live 必須同 vendor、綁定 form 且可公開；blacklist normalized | deterministic primary ID 令同 form/live/email replay 收斂為單筆；attribution 只寫第一次 | JSON 或 same-origin 303；cookie HttpOnly/Lax/30min | route unit；DB concurrent replay 本輪補強 |
| 17 | `GET /api/health` | public read-only | 無 body | 僅 DB probe 與安全分類 | 無寫入 | 200 `{ ok, database, latencyMs }`；失敗 503，不回原始 Prisma message/meta | route/logger unit + Staging/Production 歷史 runtime |
| 18 | `POST /api/jobs/webhook-retry` | timing-safe `JOB_SECRET` | 無 body | platform due queue | 先釋放過期 reservation；retry worker 使用 atomic claim，processed event 不重跑 | 401 或 bounded aggregate result | unit + isolated PostgreSQL retry regression |
| 19 | `POST /api/payments/checkout` | same-origin + client marker + 20/min rate limit | Zod vendor/product/UUID idempotency/referral，64 KiB | active product 必須屬於 vendor；attribution 只信任 server cookies/DB | SERIALIZABLE 建立 pending transaction + unique inventory reservation；同 vendor + caller key 只允許一筆 transaction，完成後 replay 保存的 checkout payload；provider/metadata 失敗會標記失敗並釋放庫存 | 400／403／404／409／429／502 或 no-store checkout contract | route + inventory concurrent duplicate unit/DB；provider Sandbox/Production 為外部 gate |
| 20 | `POST /api/security/csp-report` | public + 120/min rate limit | raw text 16 KiB | 僅 security telemetry | 目前驗證大小後丟棄；無 DB side effect | 204；超量 413 | 同路徑 unit |
| 21 | `POST /api/team-funnel/copies` | same-origin + client marker；domain `requireTeamFunnelActor` | Zod share code/mode/slug，64 KiB | active vendor membership、team、audience、share availability | claim/copy；duplicate claim 回 200，不重建第二份 | 400／404／409／500 使用穩定 error code | route + policy/domain unit |
| 22 | `POST /api/team-funnel/pages` | same-origin + client marker；domain actor policy | discriminated Zod create/copy，64 KiB | active membership、team、template version ownership | create original page 或 copy template；DB uniqueness 處理 slug/version conflict | 400／404／409／500 使用穩定 error code | route + tenant negative unit |
| 23 | `POST /api/team-funnel/partner-profile` | same-origin + client marker；domain actor policy | Zod get/team/page，64 KiB | actor 只能讀所屬 team/page | 只讀 | 400／404／409／500 使用穩定 error code | route + policy unit |
| 24 | `POST /api/team-funnel/product-slots` | same-origin + client marker；domain actor policy | discriminated Zod set-default/set-override/resolve，64 KiB；URL 2,000 chars | team、template/page、product 皆為 vendor composite relation | create/upsert/resolve；upsert key 保證同 page/slot 單筆 override | 400／404／409／500 使用穩定 error code | route + tenant/product URL unit |
| 25 | `POST /api/team-funnel/shares` | same-origin + client marker；domain actor policy | Zod create/disable，64 KiB；maxUses bounded | content owner／direct-downline／member audience policy | create/disable share；衝突回 409 | 400／404／409／500 使用穩定 error code | route + audience policy unit |
| 26 | `POST /api/team-funnel/live-shares` | same-origin + client marker；domain actor policy | Zod create/disable，64 KiB；target membership 與 expiry bounded | source page、Live、content owner、team 與 active direct-downline promoter 皆由 server 驗證 | 只保存 token hash；create 以 vendor／live／target promoter 收斂，disable 為條件更新；raw token 僅 creation response 返回 | 400／404／409／500 使用穩定 error code | route/domain unit + disposable PostgreSQL migration |
| 27 | `POST /api/team-funnel/templates` | same-origin + client marker；domain actor policy | Zod publish payload，64 KiB；content/locks bounded | template、team、content owner membership | publish immutable next version；DB unique template/version 抗 concurrent duplicate | 400／404／409／500 使用穩定 error code | route + ownership/version unit |
| 28 | `POST /api/webhooks/payments` | configured provider + raw-body signature | raw body 64 KiB；adapter normalization + `PaymentWebhookPayload` | vendor 由 verified payload 或既有 provider order 解析；scoped lookup 必須同 vendor/provider/order；amount/currency/status invariant | `WebhookEvent(provider,eventId)` DB unique；processed replay 直接成功；ledger 於 SERIALIZABLE transaction 處理 | Notify／unknown source 保留 provider JSON ack；exact PayUni payer ReturnURL 只 303 到同源 `/checkout/result` bounded outcome，不攜帶 raw identifier；400／401／403／413／500 使用安全 error code 或 payer-safe state | route fixture + isolated PostgreSQL logical-order/refund/concurrency/provider-scope regression |
| 29 | `GET /__celebratedeal_wp187_fingerprint.json`／`HEAD /__celebratedeal_wp187_fingerprint.json` | public read-only；無認證 | 無 body | 固定 Preview deployment lineage v2 payload | 無寫入；不讀 env、DB、PayUni 或 request metadata | 200；固定五欄 JSON；`Cache-Control: no-store, max-age=0`；`X-Content-Type-Options: nosniff`；HEAD 無 body | 同路徑 deterministic unit；遠端 Preview 驗證另行驗收 |
| 30 | `GET /r/[code]` | public read-only；只接受 bounded code | route param；code 最多 100 chars | server-side active `PlatformReferralCode` lookup；不接受 client owner／rate | append one platform referral click；設定 HttpOnly/Lax/30-day click cookie；無付款或 commission side effect | 307 redirect `/billing/plans`；malformed code redirect with stable error | 同路徑 deterministic unit；subscription payment callback 仍未完成 |
| 31 | `POST /api/affiliate-attribution/direct-entry` | same-origin + client marker + 60/min rate limit | 無 body | 只清除目前 request 的 server-issued attribution cookie，不讀 DB | 清除 HttpOnly attribution cookie；保留 visitor cookie；無 DB、付款或 commission side effect | 200 `{ ok: true }`、403／429；`Cache-Control: no-store` | 同路徑 unit；公開直播 direct-entry wiring unit |
| 32 | `POST /api/stream-usage` | same-origin + client marker + 120/min rate limit | strict Zod JSON；`vendorId`／`liveId`／UUID `eventId`／1–60 `watchSeconds`；可選 `sourcePageSlug` | server 驗證 vendor/live、可播放狀態與公開 Team Funnel page；attribution snapshot 由 DB 解析 | append-only immutable page/member usage ledger；`eventId` unique replay 回 `{ duplicate: true }`，payload drift 回 409；不接受 visitor identifier | 400／403／404／409／429／500 使用泛化錯誤 | route／domain／client unit；loopback migration；provider billing reconciliation 仍為人工 gate |
| 33 | `POST /api/live-admission`、`DELETE /api/live-admission` | same-origin + client marker；POST 120/min rate limit | strict Zod JSON；`vendorId`／`liveId`；server-issued opaque session cookie 不進 response body | vendor、可播放 live、quotaPolicy 與 server-owned VendorUsageLimit 由 DB 驗證 | Serializable admission；短 TTL viewer session 只保存 token hash；相同 live cookie refresh，不同 live 不共用 slot；DELETE 釋放 matching hash | 200 `{ ok }`；404／429 使用泛化錯誤；HttpOnly/Lax/short-TTL cookie | route/domain/component regression；23/23 loopback migrations；provider reconciliation 仍為人工 gate |
| 34 | `GET /api/live-playback-source` | same-origin + client marker + 120/min rate limit；HttpOnly admission cookie | bounded query；`vendorId`／`liveId` | token hash 必須對應同 vendor/live 的未過期 viewer session；live lifecycle 與 stored playback URL 由 server 驗證 | 只讀；`private, no-store`；不在 blocked response 或 public RSC props 中回傳 source | 200 `{ playbackUrl }`；400／403 使用泛化錯誤 | route/domain/page/component regression；expired／cross-vendor／cross-live negative cases |
| 35 | `POST /api/webhooks/payment-methods` | configured provider + provider-specific raw-body signature | raw body 64 KiB；adapter reduces to verified-only opaque reference contract | adapter supplies vendor/scope identity；route rechecks vendor and active membership before reference upsert | `WebhookEvent(provider,eventId)` unique；processed replay is a safe duplicate；only normalized verified reference is applied in Serializable transaction | 400／401／403／409／413／500／501 with safe codes；no raw payload response | route/domain unit；PayUni adapter and Sandbox receipt remain external gate |
| 36 | `GET /admin/billing/webhooks/[id]/reconciliation` | `requireFinanceAdmin` + platform MFA | bounded route `id`，最多 64 chars | finance admin 只能讀目前 tenant-scoped webhook event；不存在與超長 id 回安全 404 | 只讀 normalized reconciliation checks；不輸出 raw／normalized provider payload；`no-store` attachment | 404 或 sanitized JSON `{ schemaVersion, event, summary, checks }` | 同路徑 route unit；provider／staging evidence 仍為外部 gate |
| 37 | `POST /api/jobs/billing-cycle` | timing-safe `JOB_SECRET` Bearer token | 無 body；month 由 server 決定，不接受外部月份 | server-owned active vendor subscriptions 與 settlement/invoice boundary | 受保護的月結 job；Serializable settlement/invoice closure；不自動呼叫未驗證 recurring provider；錯誤只寫 sanitized monitoring | 401 或 bounded counts；失敗 503 `{ ok:false,error:"billing_cycle_failed" }` | 同路徑 route/job unit；manual payment／provider recurring evidence 仍分開驗收 |
| 38 | `POST /api/email/unsubscribe` | same-origin form request；server-signed unsubscribe token | form body 64 KiB；token 最多 512 chars | token 只解析 server-owned delivery ID；不存在與偽造 token 共用 invalid response | tenant-scoped suppression upsert，並將 queued／failed delivery 標成 suppressed | 303 到固定 done／invalid 狀態，不揭露收件人 | 同路徑 route unit |
| 39 | `POST /api/form-submissions/verify` | same-origin + client marker + 20/min rate limit | strict token form，最長 320 chars | hashed verification token 由 domain conditional claim；不存在、過期與已使用共用 invalid response | 首次驗證後可 enqueue confirmation Email；Email 失敗不回滾已完成驗證 | private no-store 303 到 verified／invalid | 同路徑 unit + disposable PostgreSQL verification regression |
| 40 | `GET`／`POST /api/jobs/email-deliveries` | `GET` 使用 timing-safe `CRON_SECRET`，供核准 scheduler（Production 若啟用 Vercel Cron）使用；`POST` 使用 timing-safe `JOB_SECRET`，供受控人工／job runner；Preview 不註冊 Vercel Cron | 無外部 payload | server-owned reminder reconciliation 與 due queue；回應不含 job、delivery、recipient 或 provider identifier | 先以 tenant/config freshness、lease、`createdAt+id` cursor 分批更新既有 VERIFIED reminder，再執行 bounded claim／delivery／retry；兩類結果各最多回傳 20 個 safe status；provider send 前再核對 current Live/template/schedule revision | 401 或 sanitized `{ ok, reconciled, reconciliationResults, processed, results }`；失敗 503 | route unit + 46 migrations／5 disposable PostgreSQL reconciliation tests；Production cron 啟用、核准 scheduler 與真實寄送仍為外部／人工 gate |
| 41 | `POST /api/live-studio/drafts` | active merchant session + same-origin/client boundary | strict draft envelope，96 KiB；draft ID 與 revision 必須成對 | live、draft、member 與 vendor 全部使用 authenticated vendor 綁定 | optimistic revision claim；30-day TTL；create／resume／save 可安全重送，conflict fail closed | bounded JSON envelope；400／409 使用 stable code | 同路徑 route unit + disposable PostgreSQL draft regression |
| 42 | `POST /api/media/images/presign` | active merchant session + same-origin/client boundary | strict filename／MIME／size，8 KiB | server 產生 object key；ImageAsset 綁 authenticated vendor | 建立 pending asset 與短效 PUT URL；provider／DB 失敗共用安全錯誤 | bounded upload contract；400／503／502 | 同路徑 route unit；R2 真實 binding 為外部 gate |
| 43 | `POST /api/media/images/complete` | active merchant session + same-origin/client boundary | strict asset ID，4 KiB | missing 與 foreign asset 共用 404；只讀目前 vendor pending asset | provider HEAD 的 size／MIME 必須吻合，條件更新成 ready | 200 ready；400／404／409／502／503 | 同路徑 route unit；R2 真實 HEAD 為外部 gate |
| 44 | `POST /api/media/videos/direct-upload` | active merchant session + same-origin/client boundary | strict title／filename／MIME／size／duration，8 KiB；大檔要求 resumable | optional Video 必須屬於 authenticated vendor；provider mapping 由 server 建立 | 建立一次 Cloudflare direct-upload mapping；不接受 client provider UID | bounded upload contract；400／404／502 | 同路徑 route unit；Cloudflare 真實 binding 為外部 gate |
| 45 | `POST /api/payments/checkout/admission` | same-origin + client marker + 20/min rate limit | strict vendor/product JSON，64 KiB | active、confirmed、internal-checkout、有庫存的 vendor/product pair | 簽發短效 admission token、server session cookie 與 checkout idempotency key | private no-store JSON；400／404／503 | 同路徑 route unit + disposable PostgreSQL admission regression |
| 46 | `GET /(app)/lives/[id]/analytics/messages/export` | active merchant session | bounded live route id | authenticated vendor/live pair | 匯出真實與 scheduled 訊息，來源欄位分流 | CSV attachment；跨 tenant 404 | 同路徑 route unit |
| 47 | `GET /api/jobs/email-deliveries` | timing-safe `CRON_SECRET` | 無 body | server-owned reminder／delivery queue | bounded reconciliation and delivery worker | 401 或 sanitized aggregate result | 同路徑 route unit + disposable PostgreSQL lifecycle |
| 48 | `GET /api/live-chat/messages` | public live availability + bounded polling | live/submission cursor query | verified viewer session and exact live binding | 只讀真實與 scheduled 訊息 | no-store bounded JSON；400／403／404 | 同路徑 route unit |
| 49 | `POST /api/live-chat/messages` | same-origin + client marker + rate limit | bounded message JSON | verified viewer、blacklist and exact live binding | 建立 viewer message；scheduled source 不可由 client 偽造 | bounded JSON；400／403／404／429 | 同路徑 route unit |
| 50 | `POST /api/media/videos/resumable-upload` | active merchant session + same-origin/client boundary | bounded file metadata and chunk contract | authenticated vendor video ownership | 建立 resumable provider upload session | bounded upload contract；400／404／502 | 同路徑 route unit；Cloudflare binding 為外部 gate |
| 51 | `POST /api/media/videos/resumable-upload/complete` | active merchant session + same-origin/client boundary | bounded video/upload identity | authenticated vendor mapping | 確認 provider upload 並更新處理狀態 | bounded completion contract；400／404／409／502 | 同路徑 route unit；Cloudflare ready 為外部 gate |
| 52 | `GET /api/media/videos/status` | active merchant session + same-origin/client boundary | bounded video id query | authenticated vendor video ownership；foreign id 共用 404 | 只讀 provider processing／ready／error 與必要 metadata；不回傳 token、stream key 或原始 provider payload | private no-store JSON；400／401／403／404 | 同路徑 route unit；Cloudflare ready 為外部 gate |
| 53 | `POST /api/orders/shipping` | native same-origin form；Server Action security、CSRF、Origin、MFA、tenant 與 CAS 由 shared helper 統一驗證 | bounded FormData；order、status、tracking 與 CSRF 欄位 | authenticated vendor／canonical order／fulfillment ownership | conditional shipping fulfillment update；重送以 CAS fail closed；成功後 303 回同源訂單頁 | 303 或安全 action error；不回傳 PII／原始錯誤 | 同路徑 route unit + commerce Browser acceptance |
| 54 | `POST /api/settings/security/mfa/confirm` | native same-origin form；shared enrollment helper 驗證 CSRF、Origin、session 與 MFA code | bounded FormData；6 位 code 與 CSRF 欄位 | current authenticated owner／platform admin session | enable MFA／consume enrollment state；成功或失敗都 303 回安全設定頁 | 303 到固定 query state；不回傳 secret／recovery code | 同路徑 route unit + MFA Browser acceptance |
| 55 | `POST /api/team-funnel/template-actions` | native same-origin form；delegates to `manageTeamFunnelTemplateAction`，沿用 CSRF、Origin、tenant 與 owner policy | bounded FormData；create／publish／create-share／disable-share action state | template、source page、team、webinar、share ownership 由既有 domain service 驗證 | create／immutable publish／share mutation；replay/conflict 由既有 service contract 處理；action-level error 以 safe JSON state 回 200，避免正常表單驗證污染 Browser console | private no-store JSON `{ status, message, ...safe fields }`；unexpected transport error 500 | 同路徑 route unit + component unit + full Browser acceptance |
| 56 | `POST /api/team-funnel/partner-page-actions` | native same-origin form；delegates to save／publish Server Actions，沿用 CSRF、Origin、tenant、field-lock 與 publish policy | bounded FormData；`operation=save|publish` 與 page content／slot fields | authenticated promoter page、template locks、team ownership 由既有 domain service 驗證 | save editable content／slot overrides／public visibility；action-level error 以 safe JSON state 回 200 | private no-store JSON `{ status, message }`；unknown operation safe error | 同路徑 route unit + component unit + full Browser acceptance |
| 57 | `POST /api/products/upsert` | native same-origin FormData；shared CSRF／Origin boundary、active merchant session、MFA 與 owner/admin policy | bounded product fields、revision、delivery metadata 與既有 CSRF token | authenticated vendor 由 session 綁定；edit target、delivery 與 product ownership 由 transport-neutral mutation service 驗證 | create／update 使用既有 transaction、revision conflict 與 delivery encryption 規則；成功 303 到固定商品列表 | 400／401／403／404／409／422 僅回 allowlisted error enum；不回 DB／delivery plaintext／credential／exception detail | 同路徑 route unit + product create/edit 10 次 Browser acceptance |
| 58 | `POST /api/billing/plans/select` | native same-origin FormData；沿用 billing plan shared core 的 CSRF、Origin、session、MFA、tenant 與 provider readiness 驗證 | bounded plan／CSRF／referral fields；超量 body 先拒絕 | authenticated vendor finance owner 與 plan／subscription scope 由 shared core 驗證；不接受 client-owned transaction 或 provider reference | 建立或重用 pending subscription／transaction；provider checkout 與 metadata failure fail closed；成功只回固定同源 303 | 303 至 allowlisted billing／auth state；400／500 僅回固定 `{ error: "checkout" }`，不回 provider、DB 或 exception detail | 同路徑 route unit + billing action unit + owner plan checkout Browser acceptance |
| 59 | `POST /api/settings/security/mfa/start` | native same-origin form；shared enrollment helper 驗證 CSRF、Origin、session 與既有 factor state | bounded FormData；CSRF 欄位 | current authenticated owner／platform admin session | 建立或重建短效 enrollment cookie；既有 factor 只回固定 `mfa_exists` state | 303 到固定 query state；不回傳 TOTP secret、recovery code 或帳號資料 | 同路徑 route unit + MFA Browser acceptance |
| 60 | `POST /api/settings/security/mfa/recovery-codes/dismiss` | native same-origin form；shared enrollment helper 驗證 CSRF、Origin、session 與 recovery-code acknowledgement state | bounded FormData；CSRF 欄位 | current authenticated owner／platform admin session | 清除短效 recovery-code acknowledgement cookie；不修改 MFA factor 或 recovery code | 303 到固定安全設定頁；不回傳 recovery code、帳號資料或原始錯誤 | 同路徑 route unit + helper unit + MFA Browser acceptance |
| 61 | `POST /api/admin/ops/payuni/wp4-session` | timing-safe `JOB_SECRET`，只允許 Preview PayUni Sandbox 的受控 WP4 runner；source SHA 必須 timing-safe 對應 deployment | 無 body；使用 source-owned deterministic synthetic vendor／owner identity 與 deployment 設定，不接受 caller identity | exact active synthetic owner membership + active synthetic user | 至多建立一個 15 分鐘 vendor-bound session，session insert 原子寫入 MFA verified timestamp；不更新 user last-login | 204 + hardened session cookie／`no-store`；未授權 401，disabled／identity mismatch 404，設定或內部失敗 503；不回傳 identity、session 或 token | 同路徑 unit；secure staging WP4 runner 為外部 gate |
| 62 | `POST /api/admin/ops/payuni/wp4-preflight` | timing-safe `JOB_SECRET`，只允許 `VERCEL_ENV=preview`、`PAYUNI_ENV=sandbox`、`WP4_SANDBOX_EXECUTOR_ENABLED=true`；40-hex source SHA 必須 timing-safe 對應 deployment | 無 body；任何 body 不消費即拒絕；只讀 source-owned deterministic synthetic vendor／owner／product／plan／invoice identity | 先驗 exact active owner membership/user；active confirmed product + positive price／inventory、無 external checkout、delivery ready 且同 vendor；active paid plan；同 vendor、positive total 且 `issued`／`overdue` invoice | owner 通過後僅三個 fixture Prisma `findFirst` projection；無 transaction、raw SQL、寫入、checkout、provider、payment 或 refund call | 200 僅 `{ ready:true, buyerOrder:true, platformSubscription:true, invoicePayment:true }` + `no-store`；未授權 401；disabled／source／fixture mismatch 404；deployment 或 DB error 503；authenticated Preview 404 只可附 `EXECUTOR_DISABLED` 或 `FIXTURE_UNAVAILABLE` closed header，不回 identity、值或 raw error | 同路徑 fail-closed unit；外部 PayUni Sandbox reconciliation 仍為外部 gate |
| 63 | `POST /api/admin/ops/payuni/wp4-fixture` | timing-safe `JOB_SECRET`，只允許 exact-source Preview PayUni Sandbox executor | 無 body、無 caller-owned identity；fixture identity 固定於 source，全部使用 synthetic invalid-domain metadata | 交易內先驗 deterministic ID 與 unique identity 不衝突；只建立或修復專用 synthetic vendor、owner membership、physical product、paid plan 與 issued invoice | 最多建立／修復 6 個 synthetic fixture units；既有 identity 任一不一致即 409 fail closed，不刪除、不讀 customer row、不呼叫 provider | 200 只回 `{ready,createdCount,reusedCount}` bounded counters；401／404／409／503 泛化回應 + `no-store`，不回 identity、資料列或 raw error | service identity-collision／idempotency unit + route auth／environment／source／body fail-closed unit；secure runner receipt 明列 setup writes |

## 已確認的 contract 缺口

| ID | 範圍 | 缺口 | 風險 | 下一個可重現證據 |
|---|---|---|---|---|
| API-C01 | Checkout | caller 沒有 idempotency key；使用者重送會建立新 order/reservation | provider 前重送可能形成多筆 pending transaction | 定義 idempotency token semantics；加入 concurrent duplicate checkout DB test |
| API-C02 | Public analytics／affiliate click | append-only endpoint 沒有 authenticity proof 或 replay key | 指標可被 same-origin automation 灌水 | 依 D-001／D-002 決策加入 signed event/referral proof |
| API-C03 | Cloudflare direct upload/live input | 每次受權呼叫都可能建立新 provider resource | job retry 可能重複建立 provider resource | provider 支援的 idempotency key 或本地 operation ledger |
| API-C04 | Test Email／Sentry／PostHog | 每次呼叫都會建立外部事件 | ops retry 會重複通知 | 僅保留受控人工 smoke；若要自動化，加入 explicit run token |
| API-C05 | Error contract | 多數 public routes 使用穩定字串，Team Funnel 使用結構化 code，尚未有 machine-readable manifest | client 對錯誤格式的假設可能漂移 | 將本 registry 對應成 executable contract cases |

## 驗收判定

- Static inventory：61/61 route handlers 已登錄。
- Same-path test：61/61。
- Runtime input validation：所有 JSON/form write route 已使用 Zod 或明確 bounded raw-body parser。
- Auth／tenant：與 `AUTHORIZATION_MATRIX.md` 一致。
- 完成度：registry 已建立；API-C01～C05 尚未關閉，因此 Q08 不能標為 100。
