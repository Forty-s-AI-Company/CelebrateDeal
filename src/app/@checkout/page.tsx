// A zero-segment match closes a previously active checkout parallel slot when
// the viewer navigates back to the site root through client-side navigation.
export default function RootCheckoutSlot() {
  return null;
}
