# Generated QA Reports

This directory is the local destination for Playwright HTML/results, screenshots, traces, videos, and Lighthouse output. Generated artifacts are ignored by Git because they may contain user-entered test data. CI uploads them as short-lived build artifacts when appropriate.
